
1.3
```scheme
(define (square x) (* x x))
(define (accmaxsq start a b) 
  (if (> a b) 
      (+ start (square a))
      (+ start (square b))))

(define (sqsumlargest x y z)
  (accmaxsq (accmaxsq 0 x y) y z))
```
